# ===========================================
# yoDEV Marketplace — Railway Environment Variables
# ===========================================

# PostgreSQL (provided by Railway)
DATABASE_URL=

# Session encryption (generate a random 32+ char string)
SESSION_SECRET=

# OIDC Authentication (discourse-oidc-bridge)
OAUTH_OIDC_ISSUER=
OAUTH_OIDC_CLIENT_ID=
OAUTH_OIDC_CLIENT_SECRET=
OAUTH_OIDC_REDIRECT_URI=

# Cloudflare R2 Storage (optional — file uploads won't work without these)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=
